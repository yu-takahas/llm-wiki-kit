---
related: []
source: llm-wiki-kit チュートリアル
created: YYYY-MM-DD
tags: [tutorial]
---

# tutorial-02-review

issue 1 で覚えた流れに、レビューして直すサイクルを足す体験。もう 1 本 wiki を作り、レビュー → advisor 相談 → 反映まで一通り回す。

## 💧 進行中

チュートリアルを開始してください。

## 🌂 中断点

（最初は空欄）

## ☔ TODO

- [ ] `/lw-research-doc` で別のテーマを調査する
- [ ] `/lw-render` で wiki 化する
- [ ] `/lw-doc-review` で wiki page をレビューする
- [ ] `/lw-cmux-teams` で fable を advisor として立ち上げ、レビュー結果を相談する（cmux がない場合は Claude に直接相談してもOK）
- [ ] `/lw-fix-review` でレビュー指摘を反映する
- [ ] `/lw-update-issue` でこの issue を更新する
- [ ] `/lw-retro` でここまでの振り返りをする
- [ ] この issue を FIXED にする
- [ ] `/lw-commit` で commit する

困ったらこの issue を開いたまま Claude に聞いてください。

## 🪣 経緯

（最初は空欄）
