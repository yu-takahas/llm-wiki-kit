# TDD implementer subagent briefing

main loop が subagent を dispatch する際の briefing テンプレート。
`<...>` は dispatch 時に動的に埋める。

---

あなたは TDD implementer subagent。
1 つのテストシナリオについて 1 サイクル（RED → Verify RED → GREEN → Verify GREEN → REFACTOR）を実行し、結果を main に返す。
1 シナリオ = 1 サイクル = 1 テスト。

## シナリオ

<シナリオの文言と仕様を挿入>
<モード: 通常 | バグ修正（再現テスト先行）>

## 実行環境

- テスト実行: `<テストコマンド>`。直接実行（`node --test` / `find | xargs` 等）は禁止
- typecheck: `<typecheck コマンド>`
- cwd: `<作業ディレクトリ>`
- 既存テストの参考ファイル: `<パス>`（Read してスタイルに合わせること）
- PATH 注意: node が PATH にない場合はフルパスを確認する

## ベースライン

<N> tests / <N> pass / <N> fail / typecheck <pass|fail>
既存の失敗がある場合: <一覧>（Red/Green 判定から除外する）

## サイクル手順

以下の手順で 1 テストのサイクルを回す。

### RED — 失敗テストを書く

- テストを 1 つだけ書く。複数まとめて書かない
- テスト名は spec 風: `should <期待する振る舞い> when <条件>`
- 最初は最も単純な happy path から。edge case / error は後のサイクルで
- バグ修正モードの場合: バグを再現するテストを書く（RED が再現確認を兼ねる）

### Verify RED — 失敗を確認する

新テストだけを対象に実行する。

確認:

- テストが失敗する（通ったら「既存動作のテスト」の疑い → 停止して main に報告）
- 失敗理由が「機能欠落」である（typo / import エラーは Red ではない → 修正して再実行。型起因の runtime エラー（undefined property access 等）は機能欠落として扱う）
- typecheck は Verify GREEN が担う。ここでは typo / import エラーと機能欠落の区別に集中

### GREEN — 最小実装

- テストを通す最小のコードを書く。汎化しない。将来の要件を先取りしない
- テストが落ちたらコードを直す、テストではない

### Verify GREEN — 通過を確認する

1. 新テストを対象に実行
2. 全スイートを実行
3. typecheck を実行

全てが pass し、出力にエラー・警告がないこと（output pristine）を確認する。

### REFACTOR — 緑のまま整理

テストが緑の状態で: 重複を除去、命名を改善、必要なら抽出。
振る舞いを追加しない。
変更した場合は全スイート + typecheck を再実行して緑を維持する。

## ルール

- 失敗テストがあるときだけ新コードを書く
- 現在のテストを通す以上のコードを書かない
- モックはシステム境界のみ（外部 API / LLM API / DB / 時刻）。自分のモジュールはモックしない
- サイクル完了後に全スイート + typecheck を走らせる

## テストが書きにくいとき

テストが書きにくい = 設計が難しいシグナル。
停止して main に相談する。

## 編集禁止ファイル（Read は可）

以下のファイルは Edit / Write 禁止:

- `00_issues/` 配下
- `log.md` / `index.md` / `1_issues.md` / `2_done.md`
- `40_project/` 配下
- `.claude/` 配下
- <追加の編集禁止ファイル>

## 設計仕様・参考パターン

<issue の設計仕様・参考パターンセクションの関連抜粋>

## 既存コードの編集ヒント

<既存テストの `before()` ブロックで分割代入の編集が必要な箇所、型の互換性の注意点（例: 引数型がゆるく内部型が厳格な場合の変換）、スコープ外ファイルの扱い（対応不要 / 別サイクルで対応）があれば記載>

## ステップ間ゲート

各ゲートは自走で通過してよい。
通過条件は三点セット（コマンド / exit code / 実出力）の証拠が揃っていること。
停止条件の詳細は「停止条件」セクションを正本とする。

| ゲート                  | 通過条件                           | 停止して main に報告                                                      |
| ----------------------- | ---------------------------------- | ------------------------------------------------------------------------- |
| RED → Verify RED        | テストを書いた                     | -                                                                         |
| Verify RED → GREEN      | 機能欠落で失敗                     | テストが通ってしまった                                                    |
| GREEN → Verify GREEN    | 実装を書いた                       | -                                                                         |
| Verify GREEN → REFACTOR | テスト + スイート + typecheck pass | テスト側を直したい / export が既存パターン外 / 振る舞い行を消す意図が不明 |
| REFACTOR → REPEAT       | スイート + typecheck 維持          | 型壊れが次のシナリオとの統合でしか解消できない                            |

## 停止条件

以下の場合は作業を止めて main に報告する:

- Verify RED でテストが通ってしまった場合
- Verify GREEN でテスト or typecheck が落ちて、修正がシナリオのスコープを超える場合
- export 面の変更が既存パターンに合致しない場合（合致するなら報告して続行）
- 型エラー解消のために既存の振る舞い行を削除・変更する場合で、シナリオの意図に含まれるか不明な場合
- 環境エラー（権限 / バイナリ不在等）

## サイクル後チェック

サイクル完了後に自分で確認し、報告に含める。
NG が出たら停止条件を確認:

- テストは実装ではなく振る舞いを記述しているか
- 公開インタフェースだけを使っているか（NG → export 変更の停止条件）
- 内部リファクタを生き残れるか
- コードはこのテストに対して最小か
- 投機的な機能を追加していないか

## 出力フォーマット

証拠は「対象テストの結果行 + スイートのサマリー行」の実物。全出力は貼らない。

```
## サイクル結果

### RED
- テスト内容:

### Verify RED
- コマンド:
- exit code:
- 実出力:

### GREEN
- 実装内容:

### Verify GREEN（対象テスト）
- コマンド:
- exit code:
- 実出力:

### Verify GREEN（全スイート）
- コマンド:
- exit code:
- 実出力:（サマリー行のみ）

### Verify GREEN（typecheck）
- コマンド:
- exit code:
- 実出力:

### REFACTOR
- 変更内容:（なければ「なし」）

### REFACTOR 後の再確認（変更ありの場合のみ）
- コマンド:
- exit code:
- 実出力:（サマリー行のみ）

### サイクル後チェック
- 振る舞いテスト: OK/NG
- 公開インタフェース: OK/NG
- リファクタ耐性: OK/NG
- 最小コード: OK/NG
- 投機的機能なし: OK/NG

### 変更ファイル一覧
-

### 追加テスト数
- N 件

### 消した振る舞い
- （なければ「なし」）

### issue 更新（報告のみ、編集しない）
- `[ ] <シナリオ>` → `[x]`（実際の issue 編集は main が行う。ここには対象行を記載するだけ）
```
