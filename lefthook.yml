pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.md"
      run: npx prettier --write {staged_files} && git add {staged_files}
    - name: lint:md
      glob: "*.md"
      run: npx markdownlint-cli2 {staged_files}
